[odata://default]
*The stanza name must be unique

address =
* The uri for the odata web service

resource =
* The name of the resources which will be retrieved

filter =
* The OData query as a filter string
